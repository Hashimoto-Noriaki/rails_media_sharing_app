<!DOCTYPE html>
<html>
<body>
  <div class="container mt-5">
    <div class="text-center">
      <h1><i class="fas fa-sign-in-alt pr-3 d-inline"></i>ログイン</h1>
    </div>
    <div class="text-center mt-3">
      <p class="text-left d-inline-block">ログインすると、<br>あなたのチャンネル作成／動画登録等ができるようになります。</p>
    </div>
    <div class="row mt-10">
      <div class="col-sm-4 offset-sm-4">
        <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <div class="form-group">
            <%= f.label :email, 'メールアドレス' %>
            <%= f.email_field :email, autofocus: true, class: "form-control",placeholder:"例:mail@gmail.com"%>
          </div>
          <div class="form-group">
            <%= f.label :password, 'パスワード(半角英数字)' %>
            <%= f.password_field :password, autocomplete: "current-password", class: "form-control",placeholder:"********"%>
          </div>
          <div class="form-group">
            <%= f.submit 'ログイン', class: "btn btn-warning btn-block mt-3" %>
          </div>
        <% end %>
        <div class="mt-2"><%= link_to '会員登録はこちら', signup_path %></div>
      </div>
    </div>
  </div>
</body>
</html>
